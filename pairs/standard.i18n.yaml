# Bilingual-pair consistency record for the `standard` preset (dsh-presets-zh).
# The full git blob hash of each side as of the last confirmed-consistent
# state; both languages carry equal authority (upstream deepseek-harness
# convention, docs/i18n/README.md). Editing either side without re-confirming
# fails the gate closed (`npm run verify-pairs`, also run by npm test and
# prepare). Blob hashes, not commit hashes: content-addressed, so the record
# recovers the exact last-confirmed text of either side.
#   agent.cordis.yml    = upstream original at pinned commit
#                         47f943859bef60e4160492346772ded9b24f765a
#                         (vendored at upstream/agent-presets/standard/agent.cordis.yml,
#                          byte-identical to the file at that commit)
#   agent.cordis.zh.yml = this port's translation
#                         (presets/standard/agent.cordis.yml)
# Re-record after a confirmed re-translation:
#   npm run verify-pairs -- --write presets/standard/agent.cordis.yml
agent.cordis.yml: f0916d999277cca1b30cad6baef3d0c672f5387c
agent.cordis.zh.yml: 900fdde9a0392f01791e39d199073af82e38c23a